---
import SectionTitle from './SectionTitle.astro';

// Opcional: datos de contacto para mostrar
const contactInfo = {
  email: 'geovanni@example.com',
  phone: '+506 1234 5678',
  linkedin: 'https://linkedin.com/in/geovanni',
  github: 'https://github.com/geovanni',
};
---

<section id="contact" class="max-w-4xl mx-auto py-20 px-6 scroll-mt-20">
  <SectionTitle title="Contacto" />

  <div class="mt-10 grid grid-cols-1 md:grid-cols-2 gap-12">

    <!-- Información de contacto -->
    <div class="text-white space-y-6">
      <p class="text-lg">¡Estoy abierto a nuevas oportunidades! Puedes contactarme a través de cualquiera de los siguientes medios o enviarme un mensaje directo usando el formulario.</p>

      <ul class="space-y-4 text-zinc-300">
        <li>
          <strong>Email:</strong> <a href={`mailto:${contactInfo.email}`} class="text-cyan-400 hover:underline">{contactInfo.email}</a>
        </li>
        <li>
          <strong>Teléfono:</strong> <a href={`tel:${contactInfo.phone}`} class="text-cyan-400 hover:underline">{contactInfo.phone}</a>
        </li>
        <li>
          <strong>LinkedIn:</strong> <a href={contactInfo.linkedin} target="_blank" rel="noopener noreferrer" class="text-cyan-400 hover:underline">Perfil LinkedIn</a>
        </li>
        <li>
          <strong>GitHub:</strong> <a href={contactInfo.github} target="_blank" rel="noopener noreferrer" class="text-cyan-400 hover:underline">Perfil GitHub</a>
        </li>
      </ul>
    </div>

    <!-- Formulario de contacto -->
    <!-- Cambia esta URL a tu endpoint de Formspree o backend -->
    <form
      action="https://formspree.io/f/yourformid"
      method="POST"
      class="bg-zinc-900/70 p-6 rounded-lg shadow-lg text-white flex flex-col gap-6"
    >
      <label class="flex flex-col">
        <span class="mb-2 font-semibold">Nombre</span>
        <input
          type="text"
          name="name"
          required
          placeholder="Tu nombre"
          class="rounded-md bg-zinc-800 border border-zinc-700 p-3 text-white focus:outline-none focus:ring-2 focus:ring-cyan-400"
        />
      </label>

      <label class="flex flex-col">
        <span class="mb-2 font-semibold">Email</span>
        <input
          type="email"
          name="email"
          required
          placeholder="tu@email.com"
          class="rounded-md bg-zinc-800 border border-zinc-700 p-3 text-white focus:outline-none focus:ring-2 focus:ring-cyan-400"
        />
      </label>

      <label class="flex flex-col">
        <span class="mb-2 font-semibold">Mensaje</span>
        <textarea
          name="message"
          rows="5"
          required
          placeholder="Escribe tu mensaje aquí..."
          class="rounded-md bg-zinc-800 border border-zinc-700 p-3 text-white focus:outline-none focus:ring-2 focus:ring-cyan-400 resize-y"
        ></textarea>
      </label>

      <button
        type="submit"
        class="bg-cyan-600 hover:bg-cyan-500 text-white font-semibold py-3 rounded-md transition-colors"
      >
        Enviar Mensaje
      </button>
    </form>

  </div>
</section>
