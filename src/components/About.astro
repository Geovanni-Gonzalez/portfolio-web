---
// components/About.astro
import { getLangFromUrl, useTranslations } from "../i18n/utils";
import { Icon } from "astro-iconify";
import { Image } from "astro:assets";
import profileImg from "../assets/profile.jpg";
import GradientTitle from "./GradientTitle.astro";
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const cvFileSpanish = "/documents/CurriculumEstudiantil.pdf";
const cvFileEnglish = "/documents/StudentCurriculum.pdf";

const techStack = [
  { name: "Python", icon: "logos:python" },
  { name: "C++", icon: "logos:c-plusplus" },
  { name: "Java", icon: "logos:java" },
  { name: "TypeScript", icon: "logos:typescript-icon" },
  { name: "React", icon: "logos:react" },
];
---

<section
  id="about"
  class="max-w-7xl mx-auto px-4 sm:px-6 py-20"
  data-aos="fade-up"
  data-aos-duration="1000"
>
  <GradientTitle title={t("about.title")} />

  <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mt-12 auto-rows-[minmax(180px,auto)]">
    <!-- 1. Profile Image Card (Vertical on mobile, square-ish on desktop) -->
    <div
      class="spotlight-card md:col-span-1 md:row-span-2 relative group overflow-hidden rounded-3xl h-80 md:h-auto shadow-lg"
      data-aos="fade-right"
      data-aos-delay="200"
    >
      <Image
        src={profileImg}
        alt="Geovanni González"
        class="absolute inset-0 w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
      />
      <div
        class="absolute inset-0 bg-gradient-to-t from-black/90 via-black/20 to-transparent opacity-80 group-hover:opacity-60 transition-opacity duration-500"
      >
      </div>

      <div
        class="absolute bottom-6 left-6 text-white z-10 translate-y-2 group-hover:translate-y-0 transition-transform duration-500"
      >
        <p class="font-bold text-2xl">Geovanni González</p>
        <p class="text-sm text-[var(--secondary)] font-medium mt-1">
          {t("hero.title")}
        </p>
      </div>
    </div>

    <!-- 2. Bio Card (Wide) -->
    <div
      class="spotlight-card md:col-span-2 md:row-span-1 rounded-3xl p-8 sm:p-10 flex flex-col justify-center relative overflow-hidden group transition-all duration-500"
      data-aos="fade-left"
      data-aos-delay="300"
    >
      <!-- Background effects -->
      <div
        class="absolute top-0 right-0 w-80 h-80 bg-[var(--primary-soft)] rounded-full blur-[100px] -translate-y-1/2 translate-x-1/2 pointer-events-none opacity-70 transition-colors duration-700"
      >
      </div>

      <div class="relative z-10">
        <h3 class="text-3xl font-bold text-text mb-6 flex items-center gap-3 tracking-tighter">
          <Icon
            name="mdi:account-circle"
            class="text-[var(--primary)] w-10 h-10 group-hover:scale-110 transition-transform duration-500"
          />
          {t("about.title")}
        </h3>
        <p
          class="text-[var(--text-secondary)] leading-relaxed mb-6 text-base sm:text-lg font-medium italic border-l-2 border-[var(--primary)] pl-6"
        >
          {t("about.description-1")}
        </p>
        <p class="text-[var(--text-secondary)] leading-relaxed mb-8 text-sm sm:text-base font-medium">
          {t("about.description-2")}
        </p>

        <a
          href={lang === "es" ? cvFileSpanish : cvFileEnglish}
          download
          class="btn-secondary group/btn relative inline-flex items-center gap-3 px-8 py-3.5 rounded-2xl hover:bg-[var(--primary)] hover:text-[var(--button-primary-text)] transition-all duration-500 overflow-hidden focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[var(--primary)]"
        >
          <div
            class="absolute inset-0 bg-gradient-to-r from-white/0 via-white/10 to-white/0 -translate-x-full group-hover/btn:translate-x-full transition-transform duration-1000"
          >
          </div>
          <Icon
            name="mdi:file-download"
            class="w-6 h-6 group-hover/btn:scale-110 transition-transform relative z-10"
          />
          <span class="font-bold text-sm tracking-tight relative z-10">{t("about.cvDownload")}</span
          >
        </a>
      </div>
    </div>

    <!-- 3. Location Card -->
    <div
      class="spotlight-card md:col-span-1 rounded-3xl p-8 flex flex-col items-center justify-center gap-6 group transition-all duration-500 relative overflow-hidden"
      data-aos="fade-up"
      data-aos-delay="400"
    >
      <!-- Radar/Grid Overlay -->
      <div
        class="absolute inset-0 opacity-[0.03] dark:opacity-[0.07] pointer-events-none group-hover:scale-110 transition-transform duration-1000"
      >
        <div
          class="absolute inset-0"
          style="background-image: radial-gradient(circle at center, var(--primary) 1px, transparent 1px); background-size: 24px 24px;"
        >
        </div>
        <div
          class="absolute inset-0"
          style="background-image: linear-gradient(rgba(249, 115, 22, 0.1) 1px, transparent 1px), linear-gradient(90deg, rgba(249, 115, 22, 0.1) 1px, transparent 1px); background-size: 48px 48px;"
        >
        </div>
      </div>

      <div
        class="w-20 h-20 rounded-full bg-[var(--primary-soft)] flex items-center justify-center relative z-10 transition-all duration-500"
      >
        <div
          class="absolute inset-0 rounded-full border border-[var(--primary)] animate-ping opacity-20"
        >
        </div>
        <Icon name="mdi:map-marker" class="w-10 h-10 text-[var(--primary)]" />
      </div>
      <div class="text-center relative z-10">
        <p class="text-xs uppercase tracking-[0.2em] font-black text-[var(--primary)] mb-2">
          {t("about.locationLabel")}
        </p>
        <p class="text-[var(--text-primary)] font-black text-2xl tracking-tighter">
          {t("about.country")}
        </p>
        <p
          class="text-[var(--text-secondary)] text-xs font-bold mt-2 max-w-[150px] mx-auto leading-tight"
        >
          {t("about.locationDetail")}
        </p>
      </div>
    </div>

    <!-- 4. Tech Stack Focus -->
    <div
      class="spotlight-card md:col-span-1 rounded-3xl p-8 flex flex-col justify-center group transition-all duration-500 relative overflow-hidden"
      data-aos="fade-up"
      data-aos-delay="500"
    >
      <div
        class="absolute -bottom-10 -right-10 w-32 h-32 bg-[var(--primary-soft)] rounded-full blur-3xl pointer-events-none opacity-70"
      >
      </div>

      <p
        class="text-xs uppercase tracking-[0.2em] font-black text-[var(--primary)] mb-6 flex items-center gap-2"
      >
        <Icon name="mdi:code-braces" class="text-[var(--primary)] w-5 h-5" />
        {t("about.coreStack")}
      </p>
      <div class="flex flex-wrap gap-4 relative z-10">
        {
          techStack.map((tech) => (
            <div
              class="flex items-center gap-3 px-4 py-2.5 rounded-xl bg-[var(--surface-elevated)] border border-[var(--card-border)] hover:bg-[var(--surface)] hover:border-[var(--primary)] transition-all duration-300 group/item hover:-translate-y-1"
              title={tech.name}
            >
              <Icon
                name={tech.icon}
                class="w-6 h-6 grayscale group-hover/item:grayscale-0 transition-all duration-500"
              />
              <span class="text-text text-sm font-bold tracking-tight">{tech.name}</span>
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>
