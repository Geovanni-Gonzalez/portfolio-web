---
import { getCollection } from "astro:content";
import { getLangFromUrl, useTranslations } from "../i18n/utils";
import GradientTitle from "./GradientTitle.astro";
import { Icon } from "astro-iconify";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const posts = (await getCollection("blog"))
  .filter((post) => post.data.lang === lang)
  .sort((a, b) => Number(new Date(b.data.date)) - Number(new Date(a.data.date)))
  .slice(0, 3); // Show latest 3

const blogBase = lang === "es" ? "/blog" : "/en/blog";
---

<section id="blog" class="max-w-5xl mx-auto py-20 px-6 scroll-mt-20">
  <GradientTitle title="Blog" />

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mt-12">
    {
      posts.map((post) => (
        <a
          href={`${blogBase}/${post.slug}`}
          class="spotlight-card group block p-6 rounded-2xl border border-[var(--card-border)] bg-[var(--card-bg)] backdrop-blur-2xl hover:border-[var(--primary)] transition-all duration-300"
        >
          <div class="flex items-center gap-2 text-xs text-[var(--secondary)] font-bold mb-3 uppercase tracking-widest">
            <Icon name="mdi:calendar" class="w-4 h-4" />
            {post.data.date
              ? post.data.date.toLocaleDateString(lang === "es" ? "es-ES" : "en-US", {
                  day: "numeric",
                  month: "long",
                  year: "numeric",
                })
              : ""}
          </div>
          <h3 class="text-xl font-bold mb-2 text-[var(--text-primary)] group-hover:text-[var(--primary)] transition-colors">
            {post.data.title}
          </h3>
          <p class="text-sm line-clamp-2 leading-relaxed text-[var(--text-secondary)]">{post.data.description}</p>
          <div class="mt-4 flex items-center gap-2 text-[var(--link)] text-sm font-bold opacity-0 group-hover:opacity-100 transition-opacity">
            {lang === "es" ? "Leer más" : "Read more"}
            <Icon name="mdi:arrow-right" class="w-4 h-4" />
          </div>
        </a>
      ))
    }
  </div>

  <div class="mt-12 text-center">
    <a
      href={blogBase}
      class="inline-flex items-center gap-2 text-sm font-bold text-[var(--link)] transition-colors hover:text-[var(--link-hover)] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-[var(--primary)]"
    >
      {lang === "es" ? "Ver todos los artículos" : "View all articles"}
      <Icon name="mdi:arrow-right" class="w-4 h-4" aria-hidden="true" />
    </a>
  </div>
</section>

